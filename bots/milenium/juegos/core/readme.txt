Turn word-wrap on to view this document properly.

mIRClog2HTML converts mIRC logs to pure HTML. Optionally, it can convert control codes (colors, bold, underlined). It's an win32 gui program.

You should see the details in the program itself.

File packed with UPX, and you should be able to decompress it with UPX also using the -d switch.